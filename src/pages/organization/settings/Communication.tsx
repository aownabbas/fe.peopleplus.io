// mui imports
import { Box, Typography } from '@mui/material'

import { useTranslation } from 'react-i18next'

const Communication = () => {
	const { t } = useTranslation()

	return (
		<Box>
			<Typography>Communication Communication</Typography>
		</Box>
	)
}

export default Communication
