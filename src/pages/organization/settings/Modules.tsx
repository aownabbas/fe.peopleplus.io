// mui imports
import { Box, Typography } from '@mui/material'

import { useTranslation } from 'react-i18next'

const Modules = () => {
	const { t } = useTranslation()

	return (
		<Box>
			<Typography>Modules Modules</Typography>
		</Box>
	)
}

export default Modules
